export interface AccordionItemProps {
  question: string;
  children: React.ReactNode;
  defaultOpen?: boolean;
}

/**
 * @startingPoint section="Components" subtitle="FAQ expand/collapse row" viewport="480x80"
 */
