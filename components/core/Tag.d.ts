export interface TagProps {
  children: React.ReactNode;
  /** default = on dark panels; light = on white background; accent = yellow highlight. */
  tone?: 'default' | 'light' | 'accent';
}

/**
 * @startingPoint section="Components" subtitle="Pill label for tags/filters" viewport="220x60"
 */
