export interface CardProps {
  children: React.ReactNode;
  padding?: number;
  /** Lift + shadow on hover. Disable for static/dark-panel cards. */
  hover?: boolean;
}

/**
 * @startingPoint section="Components" subtitle="White surface card, hover lift" viewport="300x180"
 */
